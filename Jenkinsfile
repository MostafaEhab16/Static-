pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh' echo " Hello world "'
            }
        }
          stage('upload to AWS') { 
            steps {
                sh' echo " Hello AWS "'
            }
        } 
    }
}
